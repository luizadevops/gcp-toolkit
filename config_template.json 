{
  // Configuração Global
  "project_id": "seu-id-de-projeto-gcp-real", // OBRIGATÓRIO: Substitua pelo ID do seu projeto sandbox

  // Configurações para o Caso 1: Inspetor de Firewall
  "firewall_inspector": {
    "source_ip_alert": "0.0.0.0/0",     // Faixa de IP de origem que aciona uma inspeção mais profunda.
    "flag_ingress_only": true,          // Se True, inspeciona apenas regras de INGRESS para o source_ip_alert.
    "target_tags_to_ignore": [          // Lista de tags de destino que fazem uma regra ser ignorada pelo scan.
      // "minha-tag-segura-para-ignorar",
      // "outra-tag-especial"
    ],
    "target_service_accounts_to_ignore": [ // Lista de SAs de destino que fazem uma regra ser ignorada.
      // "sa-especial@seu-projeto.iam.gserviceaccount.com"
    ],
    "permissive_rules_details": [       // Lista de critérios que definem "excessivamente permissivo".
      {
        "protocol": "all",              // Palavra-chave "all" para qualquer protocolo.
        "ports": []                     // Lista vazia para protocolo "all" significa que todas as portas são consideradas permissivas.
      },
      {
        "protocol": "icmp",
        "ports": []                     // Para ICMP, portas geralmente não são especificadas desta forma; lista vazia significa qualquer tipo de ICMP.
      },
      {
        "protocol": "tcp",
        "ports": [                      // Lista de portas TCP sensíveis ou "todas as portas".
          "1-65535",                    // Representa todas as portas TCP.
          "22",                         // SSH
          "3389",                       // RDP
          "23",                         // Telnet (inseguro)
          "3306",                       // MySQL
          "5432",                       // PostgreSQL
          "27017",                      // MongoDB
          "1433"                        // MS SQL Server
        ]
      },
      {
        "protocol": "udp",
        "ports": [                      // Lista de portas UDP sensíveis ou "todas as portas".
          "1-65535",                    // Todas as portas UDP.
          "53"                          // DNS (se aberto indiscriminadamente, pode ser usado em ataques de amplificação)
        ]
      }
    ]
  },

  // Configurações para o Caso 2: Scanner de Políticas IAM
  "iam_scanner": {
    "roles_to_flag": [                  // Papéis considerados muito permissivos para membros amplos.
      "roles/storage.admin"
    ],
    "members_to_flag": [                // Membros amplos que, se tiverem os papéis acima, devem ser sinalizados.
      "allUsers",                       // Internet pública.
      "allAuthenticatedUsers"           // Qualquer conta Google autenticada.
    ],
    "buckets_to_ignore": [              // Opcional: lista de nomes de buckets a serem pulados durante o scan.
      // "meu-bucket-publico-mas-ignorado-propositalmente"
    ]
  },

  // Configurações para o Caso 3: Relatório de Contagem de Queries (usa a chave "bigquery")
  "bigquery": {
    "query_log_file": "cost_reporter/query_execution_log.json", // Caminho para armazenar os timestamps de execução das queries.
                                                              // Relativo à raiz do projeto.
    "num_runs_per_query_set": 2,        // Quantas vezes executar o CONJUNTO inteiro de sample_queries abaixo.
                                        // (Ou o nome alternativo que você escolheu, ex: "execution_cycles")
    "sample_queries": [                 // Queries SQL de exemplo para rodar contra datasets públicos.
      "SELECT COUNT(1) FROM `bigquery-public-data.stackoverflow.users` WHERE reputation > 100000 AND location LIKE '%Canada%'",
      "SELECT author.email, COUNT(commit) as num_commits FROM `bigquery-public-data.github_repos.commits` WHERE EXTRACT(YEAR FROM author.date) = 2023 GROUP BY 1 ORDER BY 2 DESC LIMIT 3",
      "SELECT MAX(mean_temp) AS max_temp_netherlands_2023 FROM `bigquery-public-data.gsod.summary` WHERE year = 2023 AND country = 'NL'"
    ]
  }
}